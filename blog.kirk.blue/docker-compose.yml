version: '3'

services:
  apache:
    container_name: blog.kirk.blue
    image: httpd:latest
    restart: unless-stopped
    ports:
      - "8088:80"
    volumes:
      - ./htdocs:/usr/local/apache2/htdocs

